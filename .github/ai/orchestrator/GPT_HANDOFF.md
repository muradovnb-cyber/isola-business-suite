# GPT_HANDOFF.md — moved

The canonical GPT ↔ ISOLA orchestrator contract now lives at:

**[`../bridge/GPT_HANDOFF.md`](../bridge/GPT_HANDOFF.md)** (contract v2.0).

This stub exists only so older links continue to resolve. Do not add new fields here — edit the canonical file. The v1.0 shape (which this file used to describe) is a strict subset of v2.0; every URL, payload, and enum value here has an equivalent there.

See also: [`../bridge/README.md`](../bridge/README.md) for the whole bridge directory.
