{
  "task_id": "TASK-9999",
  "task": "Bridge v2.0 end-to-end smoke — create scripts/bridge-smoke.sh (a shell script that prints exactly one line \"bridge-v2-ok\" to stdout and exits 0) AND tests/bridge-smoke.test.js (Node script using ONLY the built-in \"assert\" module — no new devDependencies — that spawns the shell script and asserts stdout equals \"bridge-v2-ok\\n\" and exit code is 0). Do NOT modify any file outside scripts/ and tests/. Do NOT add anything to package.json. Task id is TASK-9999.",
  "description": "Bridge v2.0 end-to-end smoke — create scripts/bridge-smoke.sh (a shell script that prints exactly one line \"bridge-v2-ok\" to stdout and exits 0) AND tests/bridge-smoke.test.js (Node script using ONLY the built-in \"assert\" module — no new devDependencies — that spawns the shell script and asserts stdout equals \"bridge-v2-ok\\n\" and exit code is 0). Do NOT modify any file outside scripts/ and tests/. Do NOT add anything to package.json. Task id is TASK-9999.",
  "submitted_at": "2026-08-25T00:57:34Z",
  "submitted_by": "github-actions",
  "priority": "normal"
}
